- user attaches photo
- client event listener triggers
- client uploads photo to cloud storage

A:
- server listens to cloud storage change
- server calls GCP CV API 
- server calls web crawler to receive paragraphs
- server writes the paragraphs to firestore
- client reads the paragraphs from firestore and polls for it before loading the DOM
- how does the server send paragraphs to client?

B:
- client calls GCP CV API, but how?
- client calls web crawler to receive paragraphs

C:
- in the cloud storage event handler, make an AJAX HTTP call for the company summaries
- in the AJAX event handler, change the page innerHtml and load the data in dynamically

- client renders paragraphs
